export const metadata = {
  robots: { index: false, follow: true, noarchive: true },
  alternates: { canonical: "/ai-results" },
};

export default function AiResultsFullLayout({ children }) {
  return children;
}
