'use client';
import {useState} from 'react';import styles from './stadiumAiV2Admin.module.css';
const courses=['全24場','桐生','戸田','江戸川','平和島','多摩川','浜名湖','蒲郡','津','三国','びわこ','住之江','尼崎','鳴門','丸亀','児島','宮島','徳山','下関','若松','芦屋','福岡','唐津','大村'];
export default function Client(){const [courseCode,setCourseCode]=useState(1);const [asOf,setAsOf]=useState(new Date().toISOString().slice(0,10));const [msg,setMsg]=useState('');const [busy,setBusy]=useState(false);async function run(){setBusy(true);setMsg('集計中…');try{const r=await fetch('/api/admin/stadium-ai-v2/refresh',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({courseCode,asOf})});const j=await r.json();if(!r.ok||!j.ok)throw new Error(j.error||'失敗');setMsg('集計が完了しました。');}catch(e){setMsg(`エラー: ${e.message}`)}finally{setBusy(false)}}return <main className={styles.page}><h1>Stadium AI v2 集計</h1><p>1場または全場の直近1年データを再集計します。</p><label>対象<select value={courseCode} onChange={e=>setCourseCode(Number(e.target.value))}>{courses.map((x,i)=><option key={x} value={i}>{x}</option>)}</select></label><label>集計基準日<input type="date" value={asOf} onChange={e=>setAsOf(e.target.value)}/></label><button disabled={busy} onClick={run}>{busy?'集計中…':'再集計する'}</button>{msg&&<div className={styles.msg}>{msg}</div>}</main>}
