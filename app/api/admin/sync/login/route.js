import { NextResponse } from "next/server"; import { adminCookie, createAdminToken } from "../../../../admin/sync/_lib/adminAuth";
export async function POST(request){const form=await request.formData();const pass=String(form.get("password")||"");if(!process.env.ADMIN_DASHBOARD_PASSWORD||pass!==process.env.ADMIN_DASHBOARD_PASSWORD)return NextResponse.redirect(new URL("/admin/sync/login?error=1",request.url),303);const res=NextResponse.redirect(new URL("/admin/sync",request.url),303);res.cookies.set(adminCookie.name,createAdminToken(),{httpOnly:true,sameSite:"lax",secure:process.env.NODE_ENV==="production",path:"/",maxAge:adminCookie.maxAge});return res;}
