import {notFound} from "next/navigation";import BookIssueReader from "../../library/components/BookIssueReader";import {getIchikaBookSeries} from "../../../lib/ichikaBookSeries";import {getPublishedIchikaBook} from "../../../lib/ichikaBookDb";export const dynamic="force-dynamic";export default async function Page({params}){const {issue}=await params,series=getIchikaBookSeries("data-lab"),book=await getPublishedIchikaBook("data-lab",issue);if(!book)notFound();return <BookIssueReader series={series} issue={book}/>;}
