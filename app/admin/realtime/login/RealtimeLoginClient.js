"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import styles from "../realtimeAdmin.module.css";
export default function RealtimeLoginClient(){const router=useRouter();const[password,setPassword]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);setError("");try{const r=await fetch("/api/admin/schedule/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({password}),cache:"no-store"});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||"ログイン失敗");router.replace("/admin/realtime");router.refresh()}catch(err){setError(err.message||"ログイン失敗");setBusy(false)}}return <main className={styles.loginPage}><form className={styles.loginCard} onSubmit={submit}><div className={styles.loginLogo}>⚡</div><span>BOATSTRIKERS CMS</span><h1>リアルタイム更新管理</h1><p>管理パスワードを入力してください。</p>{error?<div className={styles.error}>{error}</div>:null}<input type="password" required autoComplete="current-password" placeholder="管理パスワード" value={password} onChange={e=>setPassword(e.target.value)} disabled={busy}/><button disabled={busy}>{busy?"ログイン中…":"ログイン"}</button></form></main>}
